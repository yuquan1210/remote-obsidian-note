lead-allocator:
- AllocateLeadResponse add type
- postLeadToLeadFranchiseMatchingService use post & return AllocateLeadResponse
- buildLeadEventPayload send different msg based on type

lead-store:
- add lead status enum
- TO_BE_MASKED_STATUS add new status
- update diagram (status)

lead-store only accept allocated or duplicated
use 三元 as condition
fix issue both allocator & lead store

- allocate lead response msg not used, do we need to print it in log?

test:
- merge lead-store, allocator, no lead-franchise

