- 下游下线，大量leads堆积在ALA DLQ
- ala dlq 重放困难
- 后面在lead engine 中停掉lead allocator服务，刻意将leads卡在lead engine，再来重放
- lead allocator 重放过程中 达到 SLW (email distribution service) 的邮件处理上线(与google给的上限有关)，导致19条卡住？
- lead allocator 配置了消费速率，


```
[Alert for MCHL_MINISITE_UBOOKR lead count drop to 0 within the last 12 hours]
action.webhook = 1
action.webhook.param.url = xxx
counttype = number of events
relation = equal to
quantity = 0
dispatch.earliest_time = -12h
dispatch.latest_time = now
cron_schedule = 0 0,12 * * *
enableSched = 1
search = index=fin-exp-prod ecs_service_name=enquiry-api Lead Source is MCHL_MINISITE_UBOOKR earliest=-12h latest=now
```
**这个告警每天在 00:00 和 12:00 运行一次，如果在最近 12 小时内 enquiry-api 服务中完全没有产生任何 MCHL_MINISITE_UBOOKR 的 lead 事件，就触发告警并发送一个 webhook。**
- 日志的 `_raw` 内容里同时包含  
`Lead`、`Source`、`is`、`MCHL_MINISITE_UBOOKR`
- 需要命中整句话，log 打整句话

- 测试，splunk配置 查dev
- 问题：哪个slack channel
- 问题：mortgage-choice 查询语句


app-repo for slack?