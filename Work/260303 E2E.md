alternate email need to mask in mc-ala

- campaign id (maybe other fields with default values) request missing in 
- background bq don't go thru mc-adapter so has campign id in bq
- mortgage-choice adapter
	- lead franchise matching service has the set defualt value logic
- in lead franchise matching, request to string, remove PII, print
- enableRotation in mc-ala has default value (0/1)

EnableRotationForSetFranchise field after in lead-franchise should always be false
if set as true, pre-allocated franchise (still allocate to lead) saved to last-assigned-lead, will affect later franchise order in franchise list
- check not used (only used in generate_franchise_list cf, once lead-franchise live, no more logic)


enquiry_api 400 error will save in S3 bucket, error will print to splunk but from s3 to splunk has delay
- racb org=MORTGAGE_CHOICE will give error, use REAHL_BROKER_LANDING


test thru enquiry-api, copy teminal output, find transaction id on splunk log, then find leadid

three same letter in name or email thru enquiryapi as junk

CDP check duplicate, will use first 3 letters of name 

getfranchisebypostcode is thru bda, in test, we use find in BQ exception list to find the postcode we want (can also find franchise in LE-franchises)
- can run bda endpoint to find whether available

recipient email & sms should be modified only when they have values


0304:
recipient telephone ignore
start time end time (bq le franchise) show more zeros
our franchise data returned from 3-party endpoint, we need to append?

curl 
reautok -> lockeid
json web token (jwt.io)
login on https://www.realestate.com.au/ with rea account -> token found from chrome dev tool -> application -> cookies

enquiry-api:
- will check org, enquiry type, broker directed, etc. 
- fail give 400

minor lead source id can use default, if wrong then upsert lead throw error


260304 new bug?
- enquiry_api field name will be all lowercase so fine
- but if lead-franchise-matching / mortgage choice adapter give all upper case 
- ala save/get convert id to all lowercase first then operate
- lead table & lead archive table CRUD alllead leadid lowercase

- duplication check also read leadid, make sure lowercase covered